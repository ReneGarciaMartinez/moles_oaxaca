import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Oaxaca de Antequera',
  webDir: 'www',
  
  bundledWebRuntime: false
};

export default config;
