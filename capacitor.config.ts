import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'moles_oaxaca',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
